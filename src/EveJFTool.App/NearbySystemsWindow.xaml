<Window x:Class="EveJFTool.App.NearbySystemsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find next jump system" Width="620" Height="570" MinWidth="480" MinHeight="400"
        WindowStartupLocation="CenterOwner" Background="#172235" Foreground="#E8EDF4" FontFamily="Segoe UI">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Heading}" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap"/>
        <TextBlock Grid.Row="1" Text="Jump candidates only: highsec and Pochven excluded. Cyno availability is not checked."
                   TextWrapping="Wrap" Foreground="#B9C5D6" Margin="0,8"/>
        <DockPanel Grid.Row="2" Margin="0,4,0,10">
            <TextBlock Text="Filter name" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}" Padding="6" AutomationProperties.AutomationId="NearbyFilter"/>
        </DockPanel>
        <DataGrid Grid.Row="3" ItemsSource="{Binding Matches}" SelectedItem="{Binding Selected}"
                  Foreground="#172235" Background="#F5F7FA"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  SelectionMode="Single" SelectionUnit="FullRow" EnableRowVirtualization="True"
                  ScrollViewer.CanContentScroll="True" VirtualizingPanel.VirtualizationMode="Recycling"
                  AutomationProperties.AutomationId="NearbySystemsList">
            <DataGrid.Columns>
                <DataGridTextColumn Header="System" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Distance (LY)" Binding="{Binding DistanceLightYears, StringFormat=N3}" Width="130"/>
                <DataGridTextColumn Header="Security" Binding="{Binding SecurityStatus, StringFormat=N2}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Grid.Row="4" Text="{Binding Status}" Margin="0,8" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Insert after source" Click="Insert_Click" Padding="14,7" Margin="4" IsDefault="True">
                <Button.Style><Style TargetType="Button"><Setter Property="IsEnabled" Value="True"/>
                    <Style.Triggers><DataTrigger Binding="{Binding Selected}" Value="{x:Null}"><Setter Property="IsEnabled" Value="False"/></DataTrigger></Style.Triggers>
                </Style></Button.Style>
            </Button>
            <Button Content="Cancel" IsCancel="True" Padding="14,7" Margin="4"/>
        </StackPanel>
    </Grid>
</Window>
